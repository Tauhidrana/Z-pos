DATABASE_URL="postgres://user:password@host:5432/dbname?sslmode=require"
PORT=3000
ALLOWED_ORIGINS=http://localhost:5173
NODE_ENV=development
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key
RESEND_API_KEY=re_your_resend_api_key
CLIENT_URL=http://localhost:5173
EMAIL_DOMAIN=yourdomain.com

# Comma-separated emails auto-provisioned as active OWNERs on first sign-in.
# Access is invite-only and invites can only be sent by someone who already has
# access, so a fresh database has no way in. Set this to your own address for
# the first sign-in, then clear it.
BOOTSTRAP_OWNER_EMAILS=
